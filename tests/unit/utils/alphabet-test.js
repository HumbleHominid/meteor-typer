import alphabet from 'meteor-typer/utils/alphabet';
import { module, test } from 'qunit';

module('Unit | Utility | alphabet', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = alphabet();
    assert.ok(result);
  });
});
